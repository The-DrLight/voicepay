services:
  - type: web
    name: voicepay
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: VITE_INTRON_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
